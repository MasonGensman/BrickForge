# UTF-8
#
# StudWorks Windows executable version-info resource, in the format
# PyInstaller's `version=` spec parameter expects
# (PyInstaller.utils.win32.versioninfo). Embedded into StudWorks.exe so
# Windows Explorer's file Properties dialog shows correct metadata.
#
# Keep FileVersion/ProductVersion in sync with brickforge._version by
# hand at each release -- see that module's own docstring.

VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(0, 2, 0, 0),
    prodvers=(0, 2, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0),
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          u'040904B0',
          [
            StringStruct(u'CompanyName', u'Mason Gensman'),
            StringStruct(u'FileDescription', u'StudWorks Preview'),
            StringStruct(u'FileVersion', u'0.2.0.0'),
            StringStruct(u'InternalName', u'StudWorks'),
            StringStruct(u'OriginalFilename', u'StudWorks.exe'),
            StringStruct(u'ProductName', u'StudWorks'),
            StringStruct(u'ProductVersion', u'Preview 0.2.0'),
          ]
        )
      ]
    ),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)
